import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.moviesonline.app",
  appName: "Movies Online",
  webDir: "out",
  bundledWebRuntime: false,
};

export default config;
